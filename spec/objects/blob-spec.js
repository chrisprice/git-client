var fs = require('../../lib/objects/fs'),
    blob = require('../../lib/objects/blob'),
    data = require('./data');

describe('Parsing/composing a blob', function() {
  data();

  it('Should compose a blob', function(done) {
    done();
  });

  it('Should parse a blob', function(done) {
    done();
  });

});





